# Supabase (required for /booking — set on Vercel too)
NEXT_PUBLIC_SUPABASE_URL=https://jvzfxcrysoiplswczaml.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Site URL (for reference — set in Supabase Auth URL config)
# https://www.1take.pl

# Telegram notifications (optional — bot must be admin in channel/group)
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# Resend + Supabase setup (local only — do NOT commit real keys)
# SUPABASE_ACCESS_TOKEN=sbp_...
# RESEND_API_KEY=re_...
# Run: npm run supabase:resend

# Admin account: register once at /booking with:
# email: admin@1take.records
# password: (set in Supabase Auth / your chosen password)
# SQL trigger marks this email as admin automatically.
